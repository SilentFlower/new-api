# 2026-09-11 Redis 重建与额度恢复

## 已核实原因

- 线上 NewAPI 为 `build-097cc8f`，服务时区 `Asia/Shanghai`，Redis 模式。
- `new-api` 与 `redis` 容器于 2026-09-11 13:16 重建；Redis 没有任何挂载，`appendonly=no`，RDB 位于容器内 `/data/dump.rdb`。启动日志没有加载旧 RDB；MySQL 容器及其命名数据卷保持运行。
- 个人日周键未改名。恢复前，同渠道个人日用量总和与周用量总和完全相等，且仅包含重建后累计；MySQL 保留重建前正向消费日志。

## 授权与恢复范围

用户先授权连接线上数据库排查，随后要求“你这些额度记得要加上现在这些的”。据此恢复 Claude #80、OpenAI #52 的个人及号池日周累计，采用增量加回，保留当前金额及恢复期间新请求。未改动钱包、Token 金额或 MySQL 账单。

| 渠道 | 今日历史增量（内部 quota） | 本周历史增量（内部 quota） | 今日／本周历史用户数 |
| --- | ---: | ---: | --- |
| 80 | 460750760 | 5094904288 | 21 / 28 |
| 52 | 1254454793 | 10032561609 | 31 / 36 |

- 历史截止为 `created_at < 1789103782`；日开始 `1789056000`，周开始 `1788710400`，均为上海时区。聚合仅包含 `type=2 AND quota>0`，日志时间为记账时刻而非请求开始时刻。
- 本周目标池只有两次过去自然日的日累计人工调整；没有今日目标值调整或周累计调整。不能将历史日调整加入当前周累计。
- 个人与号池共 8 个键在同一次 Lua 中加回。号池保留用户字段与 `__pool` 总值，统计起点分别补至本日／本周开始；日周保留原到期合同。
- 计划 SHA-256：`c2619a0e7d1794d905def3781c20a0d9170ba2400e9e4f1ca3300c3ae9e1a85e`。
- 实际恢复时间：**2026-09-11 13:39:26 +08:00**。幂等键 `channel_period_recovery:20260911:1789103782:80-52` 与计数在同一次原子操作写入。
- 服务器备份：`/root/new-api/backups/quota-restore-20260911/plan.json`、`receipt.json`，权限 0600。回执包含原值、增量后的值及 TTL，可审计；没有写入凭据。

## 验证证据

- 隔离 Redis 8.4 验证错误类型时所有键均不变、正向增量保留已有消费、后续消费不被重复恢复覆盖、重复执行不重复加额、TTL 与原值回执一致。
- 线上逐字段核对 8 个键的回执严格等于原值加历史增量；实时值大于等于回执，说明后续消费继续累计。两个号池总值均等于各用户汇总。
- NewAPI 实际 GET 状态确认个人日周与号池日周恢复非零、统计起点更新；财务日志只读。恢复依据是现存正向消费日志，不声称恢复不存在的日志或未记录的历史。

## 持久化后续

- 已准备并用线上 Compose 合并校验候选配置：宿主机 `/root/new-api/redis-data:/data`，命令 `redis-server --appendonly yes --appendfsync everysec`。
- 候选文件 `/tmp/channel-period-redis-persistence.yml`，尚未应用。需要先备份当前数据，再在短暂停服期间迁移数据目录；已通过异步问题请求用户确认生产服务重启。
- 现有 Redis 未挂载数据卷的问题仍存在；恢复回执已保存在宿主机，不能把计数恢复成功等同于持久化修复完成。

## 持久化实际完成结果

用户确认“现在执行持久化修复”后已完成迁移：

- 新 Redis 容器创建于 2026-09-11 13:49:37，挂载 `/root/new-api/redis-data:/data`；AOF 开启、每秒同步、写入状态正常。
- 先在线开启 AOF 并等待首次后台落盘，再暂停 NewAPI，保存快照、停止 Redis、复制完整 RDB/AOF 到宿主机并另做静态备份；仅在逐字段校验一致后启动 NewAPI。
- 迁移前后全部 **40 个额度哈希、324 个字段**完全一致，包括本轮恢复结果及迁移前新消费。
- NewAPI 于 13:49:42 启动，13:50:13 验证 healthy；公网 `/api/status` 成功，版本仍为 `build-097cc8f`。
- 原配置、原 RDB/AOF、迁移前计数及回执保存在服务器 `/root/new-api/backups/redis-persistence-20260911/`；实际配置写入既有 `docker-compose.override.yml`，后续正常 Compose 操作会继续使用。
- 持久化修复已生效，前文“候选配置／等待确认”为执行前记录。
